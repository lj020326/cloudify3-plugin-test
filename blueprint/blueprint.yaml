tosca_definitions_version: cloudify_dsl_1_0

imports:
  - http://www.getcloudify.org/spec/cloudify/3.1/types.yaml
  - https://github.com/lj020326/cloudify3-plugin-test/blob/master/plugin.yaml

node_templates:
  mock:
    type: lee.Echo
node_types:
  lee.Echo:
    derived_from: cloudify.nodes.Root
    interfaces:
      cloudify.interfaces.lifecycle:
        create:
          implementation: lee.lib.echo.create

